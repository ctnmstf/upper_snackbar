name: upper_snackbar
description: A beautiful, customizable, and animated snackbar package for Flutter that displays notifications at the top of the screen with smooth animations and extensive customization options.
version: 0.0.1
homepage: https://github.com/ctnmstf/upper_snackbar
repository: https://github.com/ctnmstf/upper_snackbar
issue_tracker: https://github.com/ctnmstf/upper_snackbar/issues
documentation: https://github.com/ctnmstf/upper_snackbar#readme

topics:
  - snackbar
  - notification
  - toast
  - alert
  - ui
  - animation

screenshots:
  - description: 'Upper Snackbar Demo'
    path: screenshots/example.gif

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
